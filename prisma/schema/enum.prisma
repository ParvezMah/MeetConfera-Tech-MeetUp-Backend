enum UserRole {
    SUPER_ADMIN
    USER
    HOST
    ADMIN
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum EventStatus {
  OPEN
  CLOSED
  CANCELED
  RUNNING
}


enum UserEventStatus {
  JOINED
  CANCELLED
  REJECTED
  WAITING
}
